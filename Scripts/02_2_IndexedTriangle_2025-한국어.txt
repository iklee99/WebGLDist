Indexed Triangle 시작하겠습니다

이번에는 삼각형을 그리는데 vertex 좌표를 바로 이용하는 것이 아니고 

index 를 이용해서 그리는 것을 해보겠습니다

지금 여기 두 개의 triangle을 이용해서 하나의 사각형을 그리려고 하거든요

그런데 앞에처럼 삼각형을 세 개의 vertex 좌표로 생각한다면

사실 6개의 좌표가 있어야 되겠죠, 하나, 둘, 셋, 하나, 둘, 셋.

근데 지금 0번 하고 2번은 그렇게 되면 두 번씩 들어가게 되겠죠, 좌표가.

그래서 그런 거를 방지하기 위해서 여기서는 겹치는 vertex 들을 중복해서 쓰지 말고

4개만 이렇게 좌표를 마련을 했습니다

4개를 가지고 있고 그 다음에 index 를 따로 뒀어요, index array를.

그래서 0, 1, 2 이게 이제 하나의 첫 번째 트라이앵글이 되고

2, 3, 0 이게 이제 두 번째 트라이앵글이 되겠습니다

그래서 요 index와 그 다음에 vertex 좌표를

한꺼번에 사용해서 그리는 방법에 대해서 얘기를 할 겁니다

그래서 Vertex Element Array Buffer 라는 것을 사용하게 되는데

그 전에 먼저 우리가 앞에서 했던 것처럼

Vertex Array Object VAO를 create 합니다

그리고 bind 를 하죠

그 다음에 Vertex Buffer Object, VBO는 마찬가지로 똑같이 생성합니다

여기서 지금 gl.ARRRAY_BUFFER 를 사용했죠

이 gl.ARRAY_BUFFER 는 coordinate, 즉 좌표값을 갖고 있는 buffer 라고 했죠

그래서 bind를 하고 그 다음에 vertices array를 copy를 해준다 라고 했습니다.

그리고 gl.vertexAttribPointer를 사용해서

shaader 가 찾아갈 위치를 정해준다고 했고

이것은 정확히 먼저 프로그램하고 똑같은 그런 코드들입니다

한 가지 과정이 더 있겠죠. 이번에 gl.ELEMENT_BUFFER

즉 EBO라는 걸 만드는데 이 Element Buffer가 바로 Index Buffer를 이야기합니다

그래서 여기서 지금 gl.createBuffer를 사용했는데

이게 VBO를 create하는 거랑 똑같아요. 함수가 똑같아요

그래서 glCreateBuffer를 사용해서

Element Buffer, Index Buffer도 생생하고

그 다음에 bind를 해주는데

여기서 지금 보면은 gl.ARRAY_BUFFER 대신에

gl.ELEMENT_ARRAY_BUFFER 를 사용했죠

네 그래서 이게 vertex position이 아니라 index라는 거를 표시하고 있는 겁니다

그래서 bind를 하고, 그 다음에 buffer data,

아까 index 0, 1, 2,  2, 3, 0, 이 들어가 있던

그 array를 copy를 해서 buffer로 copy를 하는 겁니다

자 그 다음에 우리가 볼 다음 프로그램은

그 두 가지 형태로 렌더링을 지원하는데

orange 색으로 fill 된 (칠해진) 그런 사각형을 보여 주거나

그 다음에 F키를 누르면 line으로 되어있는 삼각형 두 개가 보이게 됩니다

그러니까 결국은 shader가 두 가지가 있어야 돼요

하나는 오렌지색으로 칠하는 shader,

그 다음에 라인으로만 보이게 하는 그런 shader.

사실은 하나로도 가능하긴 한데,

여기서는 두 개의 shader를 일부러 따로따로 두었습니다

그래서 Vertex Shader Source는

두 개의 shader program에 공통적으로 쓰이게 되겠죠

vertex 좌표들이 다 똑같기 때문에

그래서 요거는 source는 하나밖에 없어요

그리고 fragment shader source

삼각형을 오렌지색으로 필하는 fragment shader의 source 이기 때문에

요거는 따로 있고 그 다음에 fragment shader source gray는

gray 라인으로 그리는 fragment shader의 소스

이렇게 두 개의 fragment shader의 소스가 준비되어 있습니다

그래서 shader program 은 program1 은 vertex shader source하고 orange

다음에 두 번째 거는 vertex하고 gray

이렇게 두 가지를 각각 링크해서 program1과 program2를 만드는 겁니다

그 다음에 렌더링하는 파트를 좀 볼까요

그러면 이 렌더에서 먼저 clear 를 해줘야 되겠죠?

gl.COLOR_BUFFER_BIT 를 clear 해주고

Is FillMode는 Fill Mode가 true냐 아니면 false냐를 가지고 있습니다

그래서 Fill Mode가 true면 오렌지색으로 칠하는 거고

아니면 Line으로 이렇게 그리는 거고

그래서 두 개의 Triangle를 Shader Program 1 오렌지로 그리게, Fill하게 된다

그래서 gl.useProgram 을 program1을 하게 됩니다

그리고 gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER) 해가지고

fillIndexBuffer를 해 주고요. index를 bind를 해 주고

그 다음에 gl.drawElements를 call을 해요

drawElement라는 거는, 앞에서는 우리가 vertex 좌표 buffer

vbo를 직접 그렸기 때문에 거기선

gl.drawArrays 라는 function을 사용했는데

여기는 gl.drawElements라는 function을 사용합니다

그래서 gl.TRIANGLES primitive는 똑같고

fillindices는 사이즈가 되겠죠, index의 개수.

그 다음에 gl.UNSIGNED_SHORT 는 type

그 다음에 0은 offset이 되겠습니다.

이렇게 하면 인덱스를 사용해서 삼각형이 그려지게 되는 겁니다.

두 개의 삼각형이 그려지겠죠

그 다음에 isFillMode 가 false면

이거는 두 개의 triangle에 edge만을 그리게 됩니다

Shader program2 를 이용해서

그래서 gl.useProgram(program2) 를 사용하게 되고

다음에 gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER, lineIndexBuffer)

이렇게 해주면 되구요

그 다음에 gl.drawElements는 gl.LINES 를 사용하죠, 이번에 primitive를.

그 다음에 index의 갯수 그 다음에 type 그 다음에 offset

이런 식으로 그려주면 되겠습니다

그래서 요약하면 VBO를 직접 그릴 때는 drawArrays를 사용하고

index를 사용해서 할 때는 drawElements를 사용해서 그려주게 된다

라는 것을 알아두시면 되겠습니다

그 다음에 Aspect Ratio를 유지하는 것 

'resize' Event Listener를 다시 한번 봐야 되겠는데요

지난번에 2번 프로그램 삼각형을 그려냈을 때

우리가 막 Resize를 하면 삼각형이 찌그러지고 그랬죠

그거는 가로 세로 비율이 유지가 안 되기 때문에 그렇게 된 거죠

그래서 우리가 그거를 유지를 하기 위해서

이 aspect ratio라는 거를 먼저 이용을 하려고 그럽니다

그래서 CANVAS_WIDTH 하고 CANVAS_HEIGHT 이 두 개는

이제 constant로 미리 우리 canvas의 오리지널 width 하고 height가 얼마다

라는 거를 프로그래밍 해 놓은 거예요

const를 정해 놓은 거고

그래서 이 width 나누기 height를 aspectRatio로 정해놨습니다

이렇게 계산을 한거죠 그리고 newWidth newHeight는

이전에 하던대로 windows.innerWidth하고 innerHeight를 일단 가져옵니다

가져왔는데 얘네들이 이 aspect ratio에 맞도록

우리가 이거를 좀 바꿔줘야 되겠죠 그래서

만약 newWidth나누기 newHeight가 aspect ratio보다 크다

이런 경우는 즉, CANVAS_WIDTH / CANVAS_HEIGHT 보다

newWidth / newHeight가 크다는 거니까

분자가 너무 큰 경우죠 newWidth가 너무 큰 경우죠

그래서 new-width를 다시

계산하면 new-width는 new-height 곱하기 aspect ratio 이렇게 되는 거고

이제 비례식 여러분들 A : B = C : D 는

"안의 것 곱한 것 =  밖의 것 곱한거" 그런 식이 있었죠? 비례식.

그것에 의해서 새로운 newWidth를 계산해 낼 수 있습니다

그 다음에 분모 (newHeight) 가 너무 큰 경우, aspectRatio 보다 작은 경우죠

이럴 경우는 newHeight가 너무 큰 경우죠

그래서 newWidth 나누기 aspectRatio 해 가지고

newHeight를 다시 계산해 주는 겁니다

그래서 새로 계산된 newWidth, newHeight를

canvas의 width, height로 assign 해주고

그 다음에 viewport를 다시 전체 canvas로 잡고

render를 call 해주면 되겠습니다

자 그래서 프로그램 3번 HelloTriangleIndexed를 한번 보겠는데

두 개의 triangle을 그려 가지고 하나의 rectangle을 그리고요

처음에 오렌지색으로 fill 되어 있는 상태여서

F키를 누르면 렌더링이 line 모드로 갔다가

다시 F키를 누르면 fill 모드로 갔다가 왔다 갔다 합니다

프로그램 소스를 보도록 하겠습니다

03_HelloTriangleIndexed.html 서 실행을 한번 시켜 보겠습니다

Open with live server 를 열어서 실행시키니까

이렇게 fill된 Rectangle이 나오죠?

여기서 F키를 누르게 되면

이렇게 toggle로 두 개의 렌더링 모드를 왔다 갔다 할 수 있습니다

F키를 누를 때마다 왔다갔다 하는 걸 볼 수 있죠

그 다음에 Resize를 한번 해볼게요

Resize를 이렇게 움직여도 얘가 이제 전체 윈도우에 맞춰지지 않고

이 비율에 맞게 그렇죠? 이 정사각형을 항상 유지하도록 이렇게 비율에 맞게

유지가 되는 것을 볼 수가 있습니다.

소스코드 JavaScript 한번 보도록 하겠습니다

그래서 canvas 읽어왔구요. 그 다음에 WebGL2 context 읽어 왔구요

여기서 지금 CANVAS_WIDTH 하고 CANVAS_HEIGHT 를

둘 다 700, 700으로 잡아 주었습니다

그리고 초기에 canvas.width 를 세팅해 주죠 constant를 그렇게 잡아 주었고

아까 그 event listener 다시 쓴 거 여기 있죠

그래서 이 700, 700의 비율이 1대 1이니까 그걸 항상 유지하도록

newWidth, newHeight를 적당히 사이즈를 맞춰서 유지하도록

그렇게 해준 거 노트에 있는 거 다시 한번 나왔습니다

그 다음에 이거는 이제 처음에 viewport 세팅하는 부분이고요

clearColor 세팅하는 부분이고

그 다음에 vertexShaderSource 한번 볼까요

이건 이제 aPosition, position만 하나 받아오는

네 여기 location = 0 이게 빠졌는데 attribute가 하나만 있을 때는

자동적으로 제로가 되거든요 그러니까 location=0 를 빼도 되겠습니다

그 다음에 gl_Position = aPosition 을 그대로 가도록 그렇게 돼 있고요

그 다음에 fragment shader에서는

FragColor 가 이게 지금 orange fragment shader 죠

이럴 때는 orange color를 그 frag color로 정하게 돼 있고

그 다음에 이 밑에 gray color는

gray color를 frag로 정하게 frag color 컬러로 정하게 그렇게 돼 있습니다

그 다음에 compileShader, createProgram 이건 뭐 앞이랑 똑같고요

그래서 우리가 두 개의 shader program을 만들었죠

vertexShaderSource 는 두 개가 똑같고

하나는 shadeSourceOrange, 두 번째 거는 shadeSourceGray를 가지고

gl.createProgram 을 한 걸 볼 수가 있습니다

그 다음에 Vertex의 좌표 Array CPU에 있는 것

이렇게 4개만 주어졌다 그랬죠. 중복된 것 피하고. 

그 다음에 Index (0, 1, 2), (2, 3, 0) 이렇게 주어졌습니다

그 다음에 line으로 그릴 때는 조금 또 다른데요

이때는 인덱스를 중복해서 그릴 수밖에 없는데 (0, 1)그리고, 그 다음에 (1, 2) 그리고

그 다음에 (2, 3) 그리고, 그 다음에 (3, 0) 그리고

그 다음에 (0, 2) 그리고 이런 식으로 그려야 되거든요

그래서 이 gl.LINE mode 일 때는 index가 또 다르게 이렇게 주어집니다 그렇죠?

그래서 두 개의 index bufffer를 만들어야 된다 라는 얘기고요

그 다음에 VAO를 만들고 바인드하고,

좌표, Vertex 좌표를 위한 VBO를 만들어서 바인드하고

그 다음에 bufferData를 해가지고 옮기고

그 다음에, vertexAttribPointer, 그 다음에 이런 거 똑같이 하고

그 다음에 fillIndexBuffer, 그 다음에 lineIndexBuffer,

Index Buffer가 두 개라고 했죠

그래서, 각각을 bind하고 그 다음에 gl.bufferData 해가지고

Index를 또 옮겨 놓습니다

그 다음에 IsFillMode를 True로 해 놨는데 여기 let을 썼죠?

let을 쓴 것은 키보드 F키를 누를 때마다

얘가 true에서 false로 toggle이 되어야 되거든요

그래서 얘는 const를 쓸 수가 없습니다

변해야 되기 때문에 값이

그래서 그럴 땐 let 그냥 일반 variable로 사용하면 되겠습니다

참고로 말씀드리면 JavaScript 에

옛날에는 여기에 var라는 거를 많이 썼는데

이 var라는 게 굉장히 좀 불안정하고 여러 가지 단점들이 많이 있어요

자세하게 설명하는 건 여기서 생략하겠는데

그래서 const 아니면 let을 사용하도록 하십시오

그 다음에 렌더 쪽에서는 이제 Fill 모드일 때는

이렇게 아까 그 program1 을 사용해서 그리고

그 다음에 Draw Element를 사용해서 Fill Indices를 사용해서 그리자

그 다음에 Is Fill 모드가 False이면은 Line으로 그리는데 program2  를 사용하고

gl.LINES 으로 그리고 line indices 를 사용해서 그리면 되겠습니다

그 다음에 keydown이라는 event listener 를 하나 더 주었는데요

이거는 뭐냐면, 키보드가 눌렸을 때 행해지는 그런 이벤트 처리 핸들러 루틴입니다

그래서 이 경우에는 지금 이게 이제 anonymous function의 파라미터죠 이벤트가

이벤트가 발생하면 그 다음에 여기 console.log를 해가지고 key pressed 해가지고

어떤 이벤트가 들어왔는지를 우리가 프린트 해 놓게 되었어요

그 다음에 그 toLowerCase로 바꿔서 혹시 대문자로 들어올지 모르니까

그래서 이벤트의 키가 'f'와 같으면 fillMode를 현재 isFillMode의 반대로

true는 false로 false면 true로 그렇게 assign을 하도록 돼 있는 겁니다

그래서 렌더링 모드가 어떻게 바뀌었다라는 거를 또

콘솔로그로 보여주고 렌더를 한 번 더 불러주면 되겠습니다

그리고 마지막에는 우리가 정상적으로 render를 call을 해줘야 되겠죠

이 순서상 그래서 여기 지금 중간중간 function이 들어가 있고

막 그 다음에 이렇게 function call 이 들어가 있어 가지고 헷갈릴 수도 있는데

function은 그냥 아무 데나 들어가도 된다 이렇게 생각하면 되구요

심지어는 지금 뭐 중간에 이렇게 들어가 있고 또 맨 앞에 들어가 있고

막 그렇지 않습니까 그렇지만 이 흐름은 위에서 아래로 똑같이 가기 때문에

여기서 지금 render를 call하고 결국은

프로그램이 끝나게 되는 건데요 끝난다는 것보다 여기는

지금 계속해서 유지가 되는 거죠 네

자 그래서 여기 지금 console log 들이 좀 몇 개 들어가 있어서

우리가 요거를 실행을 하면서 어 developer mode 로 한번 가볼게요

단축키로 컨트롤 Alt-Ctrl-i 를 눌러 가지고

이렇게 어 그 콘솔 창을 열었습니다 지금 이미 막 여러 가지가 나오고 있는데

처음 다시 리로드 해 가지고 실행을 했고요

그 다음에 여기서 f 키를 누르게 되면 이제 Key pressed가 F가 들어왔고

렌더링 모드는 Line이다 이렇게 프린트가

돼 있죠 여기 지금 unchecked 나오는 거는 이거는

그 타이밍 길어질 때마다 나오는 거기 때문에 요거는 신경쓰지 않아도 되겠습니다

그래서 여기 지금 보면은 keypressed event.E가 F가 들어왔다 라는 거를

프린트해 주고 있고요 그 다음에 렌더링 모드가 현재 라인이냐 필이냐를 지금

프린트해 주고 있죠 그래서 얘를 F를 누를 때마다 이런 식으로 우리가

새로 나오는 것을 볼 수가 있습니다 그래서 여러분들 이걸 이용해 가지고

이제 console.log를 사용해 가지고 디버깅도 할 수 있고요

여러 가지 코딩하는데 도움을 받을 수 있습니다